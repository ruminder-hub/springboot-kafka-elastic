spring:
  application:
    name: config_server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ruminder-hub/springboot-kafka-elastic-config-repo
          clone-on-start: true
    fail-fast: true
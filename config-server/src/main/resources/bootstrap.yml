spring:
  application:
    name: config_server
  cloud:
    config:
      server:
        git:
          uri: file://Users/rumindersingh/Documents/practise/kafka/springboot-kafka-elastic/config-server-repo
          clone-on-start: true
    fail-fast: true